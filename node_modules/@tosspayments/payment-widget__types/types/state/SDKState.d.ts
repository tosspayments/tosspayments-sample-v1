import { WidgetPaymentRequest } from '@tosspayments/payment-widget-types';
import { PaymentMethodCode } from '@tosspayments/payment__types';
export declare class SDKState {
    paymentMethod: PaymentMethodCode;
    paymentRequest: WidgetPaymentRequest;
    constructor();
    updatePayment(paymentMethod: PaymentMethodCode, paymentRequest: WidgetPaymentRequest): void;
    updateAmount(amount: number): void;
}
